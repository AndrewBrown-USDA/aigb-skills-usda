# TODO

## Goal
Rename the repository references, package name, and documentation to `aigb-skills-usda` across package manifests, catalog lockfile, scripts, and documentation, while untracking planning artifacts and adding `.gitignore`.

## Tasks

### 1. Update Package Manifest and Lockfile
- [x] Update `package.json` name from `usda-skills` to `aigb-skills-usda` and description/metadata.
- [x] Regenerate/update `package-lock.json` with the updated package name.

### 2. Untrack Planning Artifacts & Add Gitignore
- [x] Create or update `.gitignore` to ignore `planning/`, `.scratch/`, and `node_modules/`.
- [x] Untrack `planning/` artifacts from git index (`git rm -r --cached planning`).

### 3. Update Catalog, Scripts, and Generator
- [x] Update `scripts/mine-history.js` to reference `aigb-skills-usda` as the generating repository.
- [x] Update `catalog/skills.lock.json` metadata `generated_from.repo` to `aigb-skills-usda`.

### 4. Update Repository Documentation
- [x] Update `README.md` to use `aigb-skills-usda` branding and description while retaining `skills` CLI usage.
- [x] Update `AGENTS.md` header and repository references to `aigb-skills-usda`.

### 5. Verification & Smoke Testing
- [x] Validate `package.json` with `node -e "require('./package.json')"`.
- [x] Run `npm test` (smoke tests) to verify catalog lockfile, sync, install, and CLI operations pass cleanly.
- [x] Check `git status` to verify `planning/` is untracked and working tree is clean/expected.

## Notes
- CLI binary remains `bin/skills.js` so `npx skills` / `node bin/skills.js` continues to work.
- Working directory remains `D:\workspace\usda-skills` unless directory move is separately coordinated.


